with (Sgfd.Base) {
    var BlogService = new Sgfd.Service({
        metaName: 'BlogService',
        index: function() {
            // A function to be used in controllers etc
            // Put it into 'services' folder of your app
        }
    });
};