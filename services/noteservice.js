with (Sgfd.Base) {
    var NoteService = new Sgfd.Service({
        metaName: 'NoteService',
        index: function() {
            // A function to be used in controllers etc
            // Put it into 'services' folder of your app
        }
    });
};