with (Sgfd.Base) {
    var LoginService = new Sgfd.Service({
        metaName: 'LoginService',
        index: function() {
            // A function to be used in controllers etc
            // Put it into 'services' folder of your app
        }
    });
};