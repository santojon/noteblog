with (Sgfd.Base) {
    var BlogController = new Sgfd.Controller({
        metaName: 'BlogController',
        index: function() {
            // A function to be used in views etc
            // Put it into 'controllers' folder of your app
        }
    });
};