with (Sgfd.Base) {
    var NoteController = new Sgfd.Controller({
        metaName: 'NoteController',
        index: function() {
            // A function to be used in views etc
            // Put it into 'controllers' folder of your app
        }
    });
};